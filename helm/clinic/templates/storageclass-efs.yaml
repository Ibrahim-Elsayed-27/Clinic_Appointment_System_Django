apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc-{{ .Release.Namespace }}
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: {{ .Values.efs.fileSystemId | quote }}
  directoryPerms: "700"
reclaimPolicy: Retain
volumeBindingMode: Immediate
