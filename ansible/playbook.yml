---
- name: Configure clinic appointment infrastructure host
  hosts: all
  become: true
  roles:
    - git
    - java
    - docker
    - awscli
    - kubectl
    - helm
    - nginx_controller
    - jenkins
    - handlers
